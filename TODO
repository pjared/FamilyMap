Still have not written the fill classes

Person handler and down should be finished, but need to make sure that the array of items are correctly being JSONED

Taken out DB calls from the following services:
LoginService
Event/Person services


Priority:
    Make sure that the getUserName and generations works in fillHandler


WHERE I LEFT OFF: FINISHING FILL, NEED TO WRITE THE DATA INSERTER FOR IT